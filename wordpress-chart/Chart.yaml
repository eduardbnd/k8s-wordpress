# Helm Chart metadata file
apiVersion: v2
name: wordpress-k8s
description: My first Helm-chart for WordPress and MariaDB
type: application
version: 0.1.0        # Version of the chart itself
appVersion: "1.0.0"   # Version of the application being deployed